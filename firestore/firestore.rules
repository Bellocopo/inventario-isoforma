rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() { return request.auth != null; }
    function isAdmin() {
      return isSignedIn() && request.auth.token.role == 'admin';
    }

    match /catalog/{materialId} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    match /storage_locations/{locationId} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
      // slots são array embutido no doc — sem subcoleção stock_items
      // melhoria futura: validar request.resource.data.slots.size() <= 4
    }

    match /kardex/{logId} {
      allow read:   if isSignedIn();
      allow create: if isAdmin();
      allow update: if false;
      allow delete: if isAdmin();
    }

    match /users/{uid} {
      allow read:  if isSignedIn();
      allow write: if false;
    }

    // Sistema legado: documento único + subcoleções sob /artifacts.
    // Admin escreve tudo; viewer (reader) só lê. Wildcard recursivo cobre
    // a coleção pai e qualquer subcoleção aninhada.
    match /artifacts/{document=**} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    match /{document=**} { allow read, write: if false; }
  }
}
